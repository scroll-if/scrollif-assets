Gem::Specification.new do |spec|
  spec.name        = "liquify"
  spec.version     = '1.0.0'
  spec.summary     = "Jekyll plugin to parse a liquid tag recursively (parse the contents of a liquid tag for other liquid tags and process them)."
  spec.authors     = ["https://github.com/vividh/liquify"]
  spec.files       = ["lib/liquify.rb"]
  spec.require_paths = ["lib"]
  spec.license       = 'MIT'
end