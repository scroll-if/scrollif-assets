Gem::Specification.new do |spec|
  spec.name        = "liquefy"
  spec.version     = '1.0.0'
  spec.summary     = "Jekyll plugin to parse and render unprocessed liquid tags (workaround for including external content with local variables)."
  spec.authors     = ["https://github.com/sonirico/liquefy"]
  spec.files       = ["lib/liquefy.rb"]
  spec.require_paths = ["lib"]
  spec.license       = 'MIT'
end